<div id = "stylists">@{
  Layout = "_Layout";
}

@using Salon.Models;

<h1>Stylists</h1>

@if (@Model.Count == 0)
{
  <h3>No stylists have been added yet!</h3>
} 

<ul>
  @foreach(Stylist stylist in Model)
  {
    <li id="link">@Html.ActionLink($"{stylist.Name}", "Details", new { id = stylist.StylistId })</li>
  }
</ul>

<p id="link">@Html.ActionLink("Add new stylist", "Create")</p>
<p id="link">@Html.ActionLink("Home", "Index", "Home")</p></div>